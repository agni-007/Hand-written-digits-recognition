# Hand-written-digits-recognition
This project implements a handwritten digit recognition system using an SVM classifier and PCA for dimensionality reduction. It features an automated pipeline for Otsu’s binarization, data augmentation (rotation/translation), and GridSearchCV optimization, enabling high-accuracy predictions on raw, real-world images.
